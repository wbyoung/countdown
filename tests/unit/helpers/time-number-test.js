import {
  timeNumber
} from 'countdown/helpers/time-number';

module('TimeNumberHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = timeNumber(42);
  ok(result);
});
